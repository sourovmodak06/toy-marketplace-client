import React from 'react';
import useTitle from '../shared/hooks/useTitle';

const Register = () => {
    useTitle('Register');
    return (
        <div>
            <h2>register</h2>
        </div>
    );
};

export default Register;