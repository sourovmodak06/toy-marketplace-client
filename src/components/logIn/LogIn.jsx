import React from 'react';
import useTitle from '../shared/hooks/useTitle';

const LogIn = () => {
    useTitle('LogIn');
    return (
        <div>
            <h2>Log in</h2>
        </div>
    );
};

export default LogIn;