import React from 'react';
import useTitle from '../shared/hooks/useTitle';

const AddToy = () => {
    useTitle('Add Toy');
    return (
        <div>
            <h2>Add a Toy page ...</h2>
        </div>
    );
};

export default AddToy;