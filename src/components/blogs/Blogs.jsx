import React from 'react';
import useTitle from '../shared/hooks/useTitle';

const Blogs = () => {
    useTitle('Blogs')
    return (
        <div>
            <h2>Blog page ...</h2>
        </div>
    );
};

export default Blogs;